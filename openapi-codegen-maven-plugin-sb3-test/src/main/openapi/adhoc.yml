openapi: 3.0.0
info:

  version: 1.0

  title: Small test

  description: Used to isolate small fragments for testing
  
servers:
- url: "https://myserver.com/api"
  description: primary endpoint
- url: "https://myotherserver.com/api"
  description: secondary endpoint

paths:
  /dummy:

    get:
      responses:
        '200':
          description: ok
          
    
components:

  schemas:

    Dummy:
      type: string
      
